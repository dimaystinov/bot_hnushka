# Telegram Бот для обработки голосовых сообщений

Асинхронный Telegram-бот на **aiogram 3**, который принимает голосовые сообщения, расшифровывает их через Whisper, обрабатывает через LLM и создаёт структурированные заметки, задачи и напоминания.

## Возможности

- 🎤 Расшифровка голосовых сообщений, аудио и видео-кружков через Whisper
- 🤖 Обработка через LLM с автоматическим fallback (OpenRouter → локальная модель)
- 📝 Автоматическая классификация сообщений (собрания, напоминания, архив)
- ✅ Извлечение задач из собраний
- ⏰ Создание напоминаний
- 📚 Структурированный архив заметок
- 📋 Интуитивное меню управления
- 🔄 Очередь обработки задач

## Технический стек

- **Python 3.10+**
- **aiogram 3.x** - асинхронный фреймворк для Telegram ботов
- **faster-whisper** - быстрая расшифровка аудио
- **SQLModel** - ORM для работы с БД
- **SQLite** - база данных (можно заменить на Appwrite)
- **httpx** - асинхронные HTTP запросы к LLM API

## Установка

### 1. Клонирование и настройка окружения

```bash
cd /root/bot_hnushka
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка конфигурации

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните необходимые параметры:

```env
# Обязательные
BOT_TOKEN=[REMOVED_BOT_TOKEN]

# Whisper (локальный или через API)
WHISPER_MODEL=base
WHISPER_DEVICE=cpu
USE_OPENAI_WHISPER_API=false

# FreeQwenApi (приоритетный)
FREEWEN_API_URL=http://localhost:8000
FREEWEN_MODEL=qwen2.5-7b-instruct

# OpenRouter (fallback)
OPENROUTER_API_KEY=[REMOVED_API_KEY]
OPENROUTER_MODEL=openai/gpt-4o-mini

# Локальная LLM (последний fallback)
LOCAL_LLM_URL=http://localhost:11434
LOCAL_LLM_MODEL=llama3.2
LOCAL_LLM_API_TYPE=ollama
```

## Настройка LLM провайдеров

> 💡 **Новое:** Теперь используются **бесплатные модели OpenRouter**! См. [FREE_MODELS.md](FREE_MODELS.md) для полного списка.

### FreeQwenApi (рекомендуется)

**Проект уже установлен в `/root/FreeQwenApi`**

1. **Авторизация (обязательно):**
   
   ⚠️ **Важно:** FreeQwenApi требует **ручной вход** в браузер. На виртуальном дисплее браузер невидим, поэтому нужен один из способов ниже.
   
   **Вариант A: VNC (видеть виртуальный дисплей):**
   ```bash
   # 1. Настройте VNC
   cd /root/FreeQwenApi
   ./setup_vnc.sh
   
   # 2. Запустите VNC сервер
   export DISPLAY=:1
   Xvfb :1 -screen 0 1024x768x24 > /dev/null 2>&1 &
   vncserver :1 -geometry 1024x768 -depth 24
   
   # 3. Подключитесь VNC клиентом к your-server-ip:5901
   
   # 4. В терминале сервера запустите авторизацию
   export DISPLAY=:1
   cd /root/FreeQwenApi
   npm run auth -- --add
   # Браузер будет виден в VNC клиенте!
   ```
   
   **Вариант B: SSH X11 Forwarding (браузер на вашем компьютере):**
   
   **На сервере (уже настроено):**
   ```bash
   # Всё готово! Просто используйте скрипт:
   cd /root/FreeQwenApi
   ./auth_x11.sh
   ```
   
   **На вашем компьютере:**
   
   **Linux/Mac:**
   ```bash
   ssh -X root@your-server-ip
   # или
   ssh -Y root@your-server-ip
   ```
   
   **Windows:**
   1. Установите [Xming](https://sourceforge.net/projects/xming/) или [VcXsrv](https://sourceforge.net/projects/vcxsrv/)
   2. Запустите Xming/VcXsrv
   3. Подключитесь:
   ```bash
   ssh -X root@your-server-ip
   ```
   
   Затем на сервере запустите `./auth_x11.sh` - браузер откроется на вашем компьютере!
   
   📖 Подробная инструкция: `/root/FreeQwenApi/X11_SETUP.md`
   
   **Вариант C: Локальная авторизация + копирование (самый простой):**
   ```bash
   # 1. На вашем компьютере с GUI
   git clone https://github.com/y13sint/FreeQwenApi.git
   cd FreeQwenApi
   npm install
   npm run auth -- --add
   # Войдите в Qwen AI в браузере
   
   # 2. Скопируйте токены на сервер
   tar -czf session.tar.gz session/
   scp session.tar.gz root@your-server-ip:/root/FreeQwenApi/
   
   # 3. На сервере
   cd /root/FreeQwenApi
   tar -xzf session.tar.gz
   ```
   
   После авторизации токены будут сохранены в `session/`.

2. **Запуск сервера:**
   ```bash
   cd /root/FreeQwenApi
   npm start
   ```
   Сервер будет доступен на `http://localhost:3264/api` (по умолчанию порт 3264).

3. **Настройка в `.env` бота:**
   ```env
   FREEWEN_API_URL=http://localhost:3264
   FREEWEN_MODEL=qwen-max-latest
   ```

**Важно:** Авторизация требует графического интерфейса или X11 forwarding. Если нет доступа к GUI, используйте Docker вариант из [README FreeQwenApi](https://github.com/y13sint/FreeQwenApi).

### OpenRouter (рекомендуется - бесплатные модели!)

1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai)
2. Получите API ключ (можно использовать бесплатно!)
3. Укажите его в `.env` как `OPENROUTER_API_KEY`
4. Настройте бесплатную модель в `.env`:
   ```env
   OPENROUTER_MODEL=google/gemini-2.0-flash-exp:free
   ```

**🚀 Лучшие бесплатные модели OpenRouter:**

1. **`google/gemini-2.0-flash-exp:free`** ⭐ (используется по умолчанию)
   - Очень быстрый отклик
   - Мультимодальный (текст, изображения, видео)
   - Огромный контекст до ~1M токенов
   - Поддержка функций и кода
   - **1.5 млрд бесплатных токенов!**

2. **`meta-llama/llama-3.3-70b-instruct:free`**
   - Мощный 70B диалоговый ассистент
   - Мультиязычный (8 языков)
   - Отличный для общего ассистента
   - **2.6 млрд бесплатных токенов!**

3. **`qwen/qwen-2.5-vl-7b-instruct:free`**
   - Мультимодальная Qwen модель
   - Сильное понимание изображений и видео (20+ минут)
   - Мультиязычные подписи
   - **47 млн бесплатных токенов**

4. **`google/gemma-3-27b-it:free`**
   - Мультимодальность (vision+text)
   - До 128k контекст
   - Хорошая математика и рассуждения
   - **2.9 млрд бесплатных токенов!**

**Лимиты бесплатных моделей:**
- 20 запросов в минуту
- 200 запросов в день
- При превышении: ошибка 429 Rate limit exceeded

**Примечание:** Все модели с суффиксом `:free` полностью бесплатны! Бот использует OpenRouter API напрямую.

### Локальная LLM (Ollama) - Fallback

**Ollama уже установлен и настроен!** Модель Qwen 2.5 7B скачана.

1. **Проверка статуса Ollama:**
   ```bash
   systemctl status ollama
   # или
   ollama list
   ```

2. **Если Ollama не запущен:**
   ```bash
   systemctl start ollama
   # или
   ollama serve
   ```

3. **Доступные модели:**
   - `qwen:4b` - используется по умолчанию (Qwen 2.5 4B, легкая ~1GB)
   - `qwen2.5:7b` - более мощная модель (4.7GB)
   - `qwen2.5:3b` - еще более легкая модель
   - `llama3.2` - альтернативная модель
   - Другие модели: `ollama pull <model_name>`

4. **Настройка в `.env`:**
   ```env
   LOCAL_LLM_URL=http://localhost:11434
   LOCAL_LLM_MODEL=qwen2.5:7b
   LOCAL_LLM_API_TYPE=ollama
   ```

**Примечание:** Ollama используется как fallback, если OpenRouter недоступен. Это обеспечивает работу бота даже без интернета.

### Альтернативные локальные LLM

- **LM Studio**: используйте `LOCAL_LLM_API_TYPE=lmstudio` и укажите URL сервера
- **text-generation-webui**: используйте `LOCAL_LLM_API_TYPE=textgen` и укажите URL API

## Запуск

### Polling режим (для разработки)

```bash
python main.py
```

### Webhook режим (для продакшена)

Для использования webhook нужно настроить дополнительно. См. документацию aiogram 3.

## Использование

1. Запустите бота
2. Откройте Telegram и найдите вашего бота
3. Отправьте команду `/start`
4. Используйте `/menu` для доступа к меню
5. **Настройте язык расшифровки** (опционально):
   - Перейдите в "⚙️ Настройки" → "🌐 Язык расшифровки"
   - Выберите язык из списка (15 популярных языков + автоопределение)
   - Текущий язык отмечен галочкой ✅
6. Отправьте голосовое сообщение, аудио или видео-кружок

Бот автоматически:
- Расшифрует аудио на выбранном языке (или определит язык автоматически)
- Определит тип сообщения (собрание/напоминание/архив/дневник/работа/дом/учёба/идеи/здоровье/финансы)
- Обработает и создаст структурированный результат
- Предоставит кнопки для управления результатом

### Поддерживаемые языки

Бот поддерживает 15 самых популярных языков:
- 🇷🇺 Русский
- 🇬🇧 English
- 🇨🇳 中文
- 🇪🇸 Español
- 🇸🇦 العربية
- 🇵🇹 Português
- 🇯🇵 日本語
- 🇩🇪 Deutsch
- 🇫🇷 Français
- 🇰🇷 한국어
- 🇮🇹 Italiano
- 🇹🇷 Türkçe
- 🇮🇳 हिन्दी
- 🇵🇱 Polski
- 🇳🇱 Nederlands

Также доступен режим "Автоопределение", когда бот сам определяет язык аудио.

## Структура проекта

```
bot_hnushka/
├── bot/
│   ├── handlers/          # Обработчики команд и сообщений
│   │   ├── common.py      # Старт, меню, help
│   │   └── media.py       # Обработка голосовых/аудио
│   ├── models/            # Модели данных
│   │   └── database.py    # SQLModel модели
│   ├── services/          # Бизнес-логика
│   │   ├── whisper_service.py  # Расшифровка аудио
│   │   ├── llm_service.py      # Работа с LLM
│   │   └── queue_service.py    # Очередь задач
│   ├── storage/           # Работа с БД
│   │   └── database.py    # Инициализация БД
│   └── utils/             # Утилиты
│       └── logger.py       # Настройка логирования
├── config.py              # Конфигурация
├── main.py                # Точка входа
├── requirements.txt       # Зависимости
└── README.md             # Документация
```

## Настройка моделей Whisper

Доступные модели (от меньшей к большей):
- `tiny` - самая быстрая, наименьшая точность
- `base` - баланс скорости и точности
- `small`
- `medium` - хороший баланс точности и скорости (используется по умолчанию)
- `large-v2` - самая точная, но медленная

Для GPU используйте `WHISPER_DEVICE=cuda`.

**Примечание:** По умолчанию используется модель `medium` для лучшего качества расшифровки. Если нужна более быстрая обработка, можно переключиться на `base` или `small` в `.env`.

## Логирование

Логи сохраняются в папке `logs/` с ротацией по дням. Уровень логирования настраивается через `LOG_LEVEL` в `.env`.

## База данных

По умолчанию используется SQLite (`bot.db`). Для продакшена рекомендуется использовать Appwrite.

### Использование Appwrite

1. **Создайте проект в Appwrite:**
   - Зарегистрируйтесь на [Appwrite Cloud](https://cloud.appwrite.io) или установите локально
   - Создайте новый проект
   - Получите `Project ID` и `API Key`

2. **Настройте `.env`:**
   ```env
   USE_APPWRITE=true
   APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
   APPWRITE_PROJECT_ID=your_project_id
   APPWRITE_API_KEY=your_api_key
   ```

3. **Создайте коллекции в Appwrite:**
   - `users` - пользователи
   - `processing_tasks` - задачи обработки
   - `tasks` - задачи из собраний
   - `reminders` - напоминания
   - `archive_items` - архивные заметки
   - `diary_entries` - записи дневника
   - `work_notes` - рабочие заметки
   - `home_tasks` - бытовые задачи
   - `study_notes` - учебные конспекты
   - `ideas` - идеи
   - `health_logs` - записи о здоровье
   - `finance_transactions` - финансовые операции

4. **Запустите бота** - он автоматически подключится к Appwrite

**Примечание:** Appwrite SDK уже установлен в `requirements.txt`. Если используете SQLite, установите `USE_APPWRITE=false` или не заполняйте параметры Appwrite.

## Troubleshooting

### Ошибка "LLM недоступен"

1. Проверьте, что OpenRouter доступен или Ollama запущен
2. Проверьте настройки в `.env`
3. Убедитесь, что локальная LLM (Ollama) запущена, если используете fallback

### Ошибка расшифровки аудио

1. Проверьте, что модель Whisper загружена
2. Убедитесь, что достаточно места на диске
3. Для больших файлов используйте модель `base` или меньше

### Проблемы с БД

1. Убедитесь, что есть права на запись в директорию проекта
2. Проверьте путь к БД в `DATABASE_URL`

## Лицензия

MIT

## Автор

Создано с помощью Cursor AI

