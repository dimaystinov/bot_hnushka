# Безопасность и защита секретов

## ✅ Файлы, защищенные .gitignore

Все файлы с паролями, API ключами, токенами и другими секретами автоматически игнорируются git.

### Игнорируемые паттерны:

#### Environment файлы
- `.env`
- `.env.local`
- `.env.*.local`
- `.env.production`
- `.env.development`
- `.env.test`

#### Файлы с секретами (по расширению)
- `*.key`
- `*.pem`
- `*.p12`
- `*.pfx`
- `*.secret`
- `*.secrets`
- `*.token`
- `*.tokens`
- `*.credential`
- `*.credentials`

#### Файлы с секретами (по имени)
- `*_key`, `*_keys`
- `*_token`, `*_tokens`
- `*_secret`, `*_secrets`
- `*_password`, `*_passwords`
- `*_credential`, `*_credentials`
- `*api_key*`
- `*api_token*`
- `*bot_token*`
- `*openai_key*`
- `*openrouter_key*`
- `*appwrite_key*`

#### JSON файлы с секретами
- `*auth*.json`
- `*credentials*.json`
- `*secrets*.json`
- `*config*.json`
- `auth.json`
- `credentials.json`
- `secrets.json`

#### Конфигурационные файлы с секретами
- `config.local.*`
- `config.production.*`
- `config.secret.*`
- `*_local.*`
- `*_production.*`
- `*_secret.*`

#### Файлы аутентификации
- `.config/auth.json`
- `.config/credentials.json`
- `~/.codex/auth.json`
- `~/.codex/credentials.json`

#### Сессионные файлы
- `*.session`
- `*.sessions`
- `session.json`
- `sessions/`

## ⚠️ Важно

1. **Никогда не коммитьте файлы с секретами** в git репозиторий
2. **Используйте `.env` файл** для хранения секретов (он уже в .gitignore)
3. **Проверяйте перед коммитом**: `git status` перед `git add` и `git commit`
4. **Если случайно закоммитили секрет**:
   - Немедленно смените все закоммиченные ключи/пароли
   - Используйте `git filter-branch` или `git filter-repo` для удаления из истории
   - Или создайте новый репозиторий

## 📝 Рекомендации

### Для разработки
- Используйте `.env.example` как шаблон (он в репозитории)
- Копируйте `.env.example` в `.env` и заполните реальными значениями
- `.env` автоматически игнорируется git

### Для продакшена
- Используйте переменные окружения системы
- Или используйте секретные менеджеры (AWS Secrets Manager, HashiCorp Vault и т.д.)
- Никогда не храните секреты в коде или конфигурационных файлах в репозитории

## 🔍 Проверка перед коммитом

```bash
# Проверить, что секреты не попадут в коммит
git status

# Проверить конкретный файл
git check-ignore -v .env

# Поиск потенциальных секретов в коде (если установлен)
grep -r "api_key\|password\|token" --exclude-dir=venv --exclude-dir=.git .
```

## 📚 Дополнительные ресурсы

- [GitHub: Removing sensitive data from a repository](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository)
- [OWASP: Secrets Management](https://owasp.org/www-community/vulnerabilities/Use_of_hard-coded_cryptographic_key)

